flask
python-dotenv
openai
tiktoken
markdown
gunicorn
# Optional: Only needed if using FAISS (may not work on FreeBSD without BLAS libraries)
# numpy<2.0.0
# faiss-cpu
# Pure Python solution (recommended): Only needs 'openai' - no other dependencies!

